<Window x:Class="WpfRichTextAdvanced.Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:WpfRichText;assembly=WpfRichText" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfRichTextAdvanced.Example"
        mc:Ignorable="d"
        Title="MainWindow" Height="256.805" Width="673.077">
    <Grid>
        <Controls:RichTextEditor x:Name="sampleEditor" 
								Grid.Row="2"
								Text="{Binding Text, Mode=TwoWay}" 
								IsContextMenuEnabled="True" 
								IsToolBarVisible="True" 
								IsReadOnly="False"								
								Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
								Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
								 BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"
			/>

    </Grid>
</Window>
